apiVersion: v1
kind: Service
metadata:
  name: {{ include "micro-helm-chart.fullname" . }}
  labels:
    {{- include "micro-helm-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "micro-helm-chart.selectorLabels" . | nindent 4 }}
